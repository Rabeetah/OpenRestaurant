var express = require('express');
var router = express.Router();
  const authnetController = require("../../controllers/payment/authnetController");

/* /api/payment/authnet/pay. */
  // router.post("/pay/creditCard",authnetController.creditcCard);

module.exports = router;
